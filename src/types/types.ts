export interface IHeaderItem {
  image: string;
  name: string;
  alias: string;
}

export interface ICityGroups {
  [key: string]: string[];
}

export interface INavbarItem {
  title: string;
  alias: string;
}
